pluginManagement {
    repositories {
        mavenCentral()
    }
}

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("../../../../gradle/libs.versions.toml"))
        }
    }
}
